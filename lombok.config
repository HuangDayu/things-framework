# 强制为所有 @Data/@Value 类生成无参构造函数
#lombok.data.constructor.extraPrivate = true
#lombok.anyConstructor.addConstructorProperties = true
# 覆盖默认行为，即使有 final 字段也生成无参构造函数
#lombok.noArgsConstructor.force = true