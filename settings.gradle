rootProject.name = 'things-framework'


String[] modulesPath = ["/", "/things-support", "/things-support/things-starter"]
for (final def module in modulesPath) {
    file("${module}").eachDirMatch(~/things-.*/) {
        include "${module}".replaceAll("/", ":") + ":${it.name}"
    }
}